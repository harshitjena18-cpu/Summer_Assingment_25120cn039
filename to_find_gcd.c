#include<stdio.h>
int main(){
    int a,i,b;
    printf("Enter the number to be checked: \n");
    scanf("%d",&a);
    scanf("%d",&a);
    for(i=0;;i++){
        if(a%i==0 && b%i==0);
        printf("The GCD of %d  and %d is %d",a,b,i);

    }
    return 0;
}